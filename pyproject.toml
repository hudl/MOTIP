[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "motip"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["rfdetr>=1.3.0"]

[tool.hatch.build.targets.wheel]
packages = ["models", "data", "utils", "configs", "log", "structures"]
